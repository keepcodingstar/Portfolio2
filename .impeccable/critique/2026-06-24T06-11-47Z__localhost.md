---
target: "home / altitude journey (http://localhost:3000/)"
total_score: 29
p0_count: 0
p1_count: 0
timestamp: 2026-06-24T06-11-47Z
slug: localhost
---
# Critique — Home (the altitude journey), http://localhost:3000/ — re-run after P1 fixes

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Preloader, dual scroll cues, side-nav active states, count-ups. Unchanged. |
| 2 | Match System / Real World | 3 | Altitude metaphor reads; the deliberate scroll-up inversion remains a calculated risk. |
| 3 | User Control and Freedom | 3 | Side-nav jumps, "back to the sky", reduced-motion path. |
| 4 | Consistency and Standards | 3 | Home journey is cohesive; the /work case pages carry radius drift (27 one-off radii vs the 4-step scale). |
| 5 | Error Prevention | 3 | n/a-leaning on the home journey. |
| 6 | Recognition Rather Than Recall | 2 | HUD nav labels (VOID/SPACE/SKY/WORK/GROUND) + inverted scroll still impose orientation load (P2, unaddressed by choice). |
| 7 | Flexibility and Efficiency | 3 | Side-nav jump links; focusable controls. |
| 8 | Aesthetic and Minimalist Design | 3 | **Up from 2** — the cloud-vs-content collision is resolved; the two signature surfaces now read cleanly. Held back from 4 by the work-zone editorial-lane proximity + eyebrow cadence. |
| 9 | Error Recovery | 3 | n/a-leaning on the home journey. |
| 10 | Help and Documentation | 3 | Scroll cues + side-nav as ambient guidance. |
| **Total** | | **29/40** | **Good** — both P1s cleared; remaining ceiling is the two P2s you deferred. |

## Anti-Patterns Verdict

**Still genuinely distinctive — and now cleaner.** The signature flourish (the cloud layer) no longer fights the content: a reading-lens mask carves the hero headline clear and the work clouds are pushed to the margins. The "how was this made?" reaction holds; brand slop test passes.

**Deterministic scan (post-fix):** 126 findings, but **125 are advisory** — the `design-system-color` (98) and `design-system-radius` (27) rules now fire because DESIGN.md exists and documents a tight token set; they flag the atmospheric gradient stops and the /work case-page radii, not defects. **One warning** remains: the `side-tab` border (`border-left: 3px solid var(--ion)`) at `app/work/work.css:492` — the genuine ban, still on the `/work` case pages, still worth rewriting.

**Lane caution (unchanged):** the work zone still reads as the saturated editorial-typographic lane (ruled rows + tabular numerals + mono labels + italic serif). This is the deferred P2.

## Overall Impression
The fix worked. The biggest opportunity from last run — clouds covering the hero pitch and the work proof — is gone, and the journey now reads top-to-bottom without a legibility tax. The score only moves 28→29 because the P1s lived mostly in one heuristic (Aesthetic) plus persona impression; the structural ceiling is now the two P2s you chose to defer: differentiating the work zone from the editorial lane, and lowering the first-timer orientation cost.

## What's Working
1. **The hero finally lands.** "Design for outcomes, numbers that move." reads in the first second — the value prop is no longer obscured, and the clouds frame rather than cover. This is the single most important fix for the primary persona.
2. **The work proof is clean.** Metrics and titles are unobstructed; the deep-teal numerals (already AA-safe `#0f6e8e`) read with the aurora unit and soft halo as intended — an altimeter readout, not a buried stat.
3. **Distinctive system, now documented.** PRODUCT.md + DESIGN.md + sidecar mean the cool ion/aurora identity, the light-flip ink, and the canopy-glass material are now enforceable, not just present.

## Priority Issues

- **[P2] Work zone reads as the saturated editorial-typographic lane.** Ruled rows + tabular numerals + mono labels + italic serif is the fingerprint that has flooded Stitch/Notion-adjacent brand surfaces. The spatial concept lifts it, but it's the one fold that follows the crowd. **Fix:** push it toward the unique "instrument-readout / altimeter" treatment (your chosen direction) — gauge framing, tick marks, a readout that only this site would make — rather than the generic ledger. **Suggested command:** /impeccable typeset (or a scoped craft on the work zone)
- **[P2] Orientation cost: HUD labels + inverted scroll.** A first-timer must learn that scroll-*up* reveals more and decode abbreviated nav labels (VOID/SPACE/SKY/WORK/GROUND). The hero now reads, which helps, but the nav still leans cryptic. **Fix:** pair each HUD label with its plain word on hover/active; make the up-scroll affordance unmissable on first load. **Suggested command:** /impeccable clarify
- **[P3] /work case-page hygiene.** One real `side-tab` ban (`work.css:492`) plus 27 one-off radii that drift from the documented 4-step `rounded` scale (sm 2 / md 11 / lg 18 / xl 20). Not on the home journey, but it's the next surface a hiring lead opens. **Fix:** rewrite the stripe (full border / tint / leading index) and consolidate radii to tokens. **Suggested command:** /impeccable polish app/work
- **[P3] Eyebrow cadence.** The uppercase mono kicker still sits above space / work / ground. Voice-leaning (altitude copy + HUD system) but at the edge; consider dropping it on one or two folds to break the per-section reflex. **Suggested command:** /impeccable typeset

## Persona Red Flags

**Priya — Design Director (primary audience).** The legibility tax is gone: she reads the pitch and sees the proof immediately. Her remaining friction is *recognition*, not *reading* — the work zone, though clear, looks like a pattern she's seen on many brand sites, and the inverted scroll asks her to learn a convention on the clock. Bounce risk is materially lower; the differentiation now rests on the deferred P2.

**Jordan — First-Timer.** Now passes the 5-second "what is this?" test (hero reads). Residual friction: the "VOID" nav label and the unexplained scroll-up affordance.

**Casey — Mobile.** *Still unverified.* The WebGL cloud field + black-hole shader load cost and large `clamp()` headline overflow on narrow widths remain unchecked. A mobile capture/adapt pass is the highest-value unknown.

## Minor Observations
- The cloud reading-lens is a render-independent fix (CSS mask), so it holds regardless of WebGL timing — robust.
- The hero scrim is a cool ice tint, on-palette (no warm drift).
- The 125 advisory color/radius findings are mostly the atmosphere; if you want the hook quiet, formally ignore the atmospheric values, or fold a couple of genuinely-reused ones into DESIGN.md.

## Questions to Consider
- The clouds now frame the content everywhere (the mask is global). Is the centre-clear lens an asset on the descent too, or only wanted at the hero?
- What would the work zone look like if it were *only* an altimeter — gauges, ticks, a needle — and shed the ruled-ledger entirely?
- Is the inverted scroll worth the orientation cost, or would a one-time hint (or a subtle auto-nudge) pay for itself?
